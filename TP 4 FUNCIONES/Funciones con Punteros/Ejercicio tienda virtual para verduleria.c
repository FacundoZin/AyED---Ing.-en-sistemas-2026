#include <stdio.h>

/*2- Una tienda automatizada dispone de 100 kg. de judías, 100 kg de lentejas y 100 kg de*/
/*	garbanzos. 

Generar una función que:
 tenga un Menu (cargar compra, visualizacion de stock). 
 1)(cargar compra)
   1.1)pregunte la cantidad quiere de ese producto 
   1.2) responder de dos formas distintas: 
      1.2.1) (si no hay stock) se informa que no hay stock disponible de ese producto
	  1.2.2) (si hay stock) solicitar el precio del producto. 

Utilizar una función que me permita actualizar y*/
/*		mostrar el stock de los productos y calcular el precio de la comprar para ese cliente.*/
/*		Cuando termine de ingresar los productos que quiera el cliente, mostrar el importe a*/
/*		abonar, preguntar con cuánto va a pagar (verificar que sea mayor que el importe a abonar)*/
/*		y calcular el vuelto. Se deben atender N clientes.*/
/*		Nota: Se debe enviar a la función el stock de los productos y regresar stock actualizado*/
/*		e importe a abonar por el cliente. */

int main() {
	return 0;
}

float CalculadoraStockYcompra()
